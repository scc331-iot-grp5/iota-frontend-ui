import { Button } from '@mui/material';
import React from 'react';

/**
 * @return {JSX.Element} the user management overview page
 */
export default function User(): JSX.Element {
  return (
    <React.Fragment>
      <Button>hyello</Button>
    </React.Fragment>
  );
}
